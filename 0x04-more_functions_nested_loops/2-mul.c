#include "holberton.h"
/**
 * mul - multiplais two numbers.
 * @a: first number t multiply.
 * @b: second number to multiply.
 *
 * Return: Operations result.
 */
int mul(int a, int b)
{
	return (a * b);
}
